# personal-os/domain-intel runtime dependencies (Phase 3)
# Install: pip install -r requirements.txt
feedparser>=6.0     # RSS parsing in skills/youtube-scan/scripts/discover_videos.py
requests>=2.28      # HTTP for Innertube + transcript XML in skills/youtube-scan/scripts/harvest_transcripts.py + scripts/fetch_url.py

# Test-only dependencies (not required at runtime)
# rank-bm25>=0.2    # Reference comparison in scripts/test_bm25_relevance.py only
# pytest>=7.0       # Test runner
